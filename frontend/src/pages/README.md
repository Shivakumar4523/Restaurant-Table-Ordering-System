# Page Feature Modules

This project uses the Next.js App Router in `src/app` for actual routes. The folders here mirror the requested product
areas and are reserved for page-level feature modules if a screen grows beyond a single route file.
